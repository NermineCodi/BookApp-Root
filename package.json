{
  "name": "root",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Nermine <nermine@codi.tech>",
  "license": "MIT",
  "scripts": {
    "backend": "cd ../backend && yarn start",
    "frontend": "cd ../frontend && yarn start",
    "start": "concurrently --kill-others-on-fail --names \"backend,frontend\" \"yarn run backend\" \"yarn run frontend\"",
    "postinstall": "cd backend && yarn install && cd .. && cd frontend && yarn install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
 
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
